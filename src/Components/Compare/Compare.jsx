import React, {Component} from 'react';

export default class Compare extends Component {

    render() {
        return(
            <React.Fragment>
                compare
            </React.Fragment>
        );
    }
}